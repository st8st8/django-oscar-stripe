from __future__ import unicode_literals
from django.conf.urls import url
import views


app_name = "oscar_stripe_sca"
urlpatterns = [
    # url(r'webhook/$',
    #     views.StripeWebhookView.as_view(), name='webhook'),
]

